name: Keep Supabase Alive

on:
  schedule:
    - cron: '0 12 */3 * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          curl -s https://szgpqouxkxwwyfrbrlab.supabase.co/functions/v1/kiwify-webhook \
            -o /dev/null -w "Status: %{http_code}"
